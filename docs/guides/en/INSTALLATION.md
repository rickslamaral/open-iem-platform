# Linux Installation

> 🚧 In preparation. Do not run in production. Real installation still requires dedicated Linux host validation.

See [`scripts/install.sh`](../../../scripts/install.sh) and use a full commit SHA only. Real audio and Raspberry Pi 5 remain pending.
